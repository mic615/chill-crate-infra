apiVersion: v1
kind: PersistentVolumeClaim
metadata: 
  name: minio-pvc
  namespace: chill-crate
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi